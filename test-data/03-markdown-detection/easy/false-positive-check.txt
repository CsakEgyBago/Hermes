It costs $5 * 2 for the combo deal, but that is not much of a bargain really. A single asterisk like that is just multiplication, not emphasis, so this paragraph should not be flagged as Markdown. There's also a stray greater-than sign > buried mid-sentence here, which shouldn't count as a blockquote since it never starts a line.

A few more near-misses are worth including here too. The formula 3 * 4 = 12 shows up again later in the note, still just plain multiplication. Someone typed "5 > 3" earlier in a different draft of this note to compare two numbers, which is another case of a markdown-looking character used for an entirely ordinary purpose. None of these should trip any Markdown heuristic, since none of them involve a line actually starting with a special character, a matched pair of double asterisks, backticks, or a real link.
